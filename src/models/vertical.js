class Vertical {
    constructor(id, prompt, icon, iconFamily){
        this.id = id;
        this.prompt = prompt;
        this.icon = icon;
        this.iconFamily = iconFamily;
    }
}

export default Vertical 